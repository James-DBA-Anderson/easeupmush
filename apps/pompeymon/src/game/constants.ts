/** Game Boy Advance native size — scaled up by Phaser. */
export const GBA_W = 240;
export const GBA_H = 160;

/** HUD / menus. Field sprites use y as depth, so this must sit above the tallest map. */
export const UI_DEPTH = 1800;
